---
import Section from './Section.astro';
import { aboutParagraphs } from '../data/site';
---

<Section id="about" label="About" number="01">
  <div class="mx-auto max-w-3xl space-y-5 text-base leading-relaxed text-[color:var(--color-text)] sm:text-lg">
    {aboutParagraphs.map((p) => <p>{p}</p>)}
  </div>
</Section>
